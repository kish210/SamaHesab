# راهنمای نصب و راه‌اندازی سامانه سما حساب

## پیش‌نیازها

| نرم‌افزار | نسخه |
|-----------|------|
| Windows | 10/11 (64-bit) |
| .NET 9 Runtime | 9.0.0 یا بالاتر |
| SQL Server | 2019/2022 |
| RAM | حداقل ۴ گیگابایت |
| فضای دیسک | حداقل ۵۰۰ مگابایت |

## مراحل نصب

### ۱. نصب پایگاه داده

```bash
# اجرای اسکریپت‌های SQL
sqlcmd -S localhost -E -i database\08_RunAll.sql
```

یا در SQL Server Management Studio فایل `database\08_RunAll.sql` را اجرا کنید.

### ۲. تنظیم رشته اتصال

فایل `appsettings.json` را ویرایش کنید:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=YOUR_SERVER;Database=SamaHesab;..."
  }
}
```

### ۳. بیلد پروژه

```bash
build.bat
```

یا با Visual Studio 2022:
- فایل `SamaHesab.sln` را باز کنید
- پروژه را بیلد کنید (Ctrl+Shift+B)
- F5 برای اجرا

### ۴. ورود به سیستم

- نام کاربری: `admin`
- رمز عبور: `admin123`

## ساختار ماژول‌ها

```
سما حساب
├── حسابداری
│   ├── اسناد حسابداری (سند، قطعی، دائمی، برگشت)
│   ├── دفتر حساب‌ها (۴ سطح نامحدود)
│   ├── بانک و صندوق
│   ├── مدیریت چک
│   └── گزارش‌های مالی
├── انبارداری
│   ├── مدیریت کالا (بارکد، سریال، بچ، انقضا)
│   ├── انبارها (نامحدود)
│   ├── ورود/خروج/انتقال
│   └── تعدیل موجودی
├── فروش
│   ├── فاکتور فروش
│   ├── برگشت فروش
│   └── پیش‌فاکتور
├── خرید
│   ├── فاکتور خرید
│   └── برگشت خرید
├── صندوق (POS)
│   ├── تاچ اسکرین
│   ├── بارکدخوان
│   └── پرینتر رسید
├── مدیریت مشتریان (CRM)
│   ├── پروفایل مشتری
│   ├── باشگاه وفاداری
│   └── پیامک
├── منابع انسانی
│   ├── پرسنل
│   ├── حضور و غیاب
│   └── حقوق و دستمزد
└── تنظیمات
    ├── شرکت و شعب
    ├── کاربران و دسترسی‌ها
    └── پشتیبان‌گیری
```

## کلیدهای میانبر

| کلید | عملکرد |
|------|---------|
| F2 | فاکتور فروش جدید |
| F3 | صندوق POS |
| F5 | ذخیره |
| F9 | قطعی کردن |
| F12 | داشبورد |
| Ctrl+P | چاپ |
| Ctrl+E | اکسل |

## معماری نرم‌افزار

```
Presentation (WPF + MVVM)
    ↓
Application (CQRS + MediatR)
    ↓
Domain (Clean Architecture)
    ↓
Infrastructure (EF Core + SQL Server)
```

## پشتیبانی

- ایمیل: support@samaerp.ir
- وب‌سایت: www.samaerp.ir
- تلفن: ۰۲۱-۱۲۳۴۵۶۷۸

---
نسخه ۱.۰.۰ | ۱۴۰۳
